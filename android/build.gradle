buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Otras dependencias
        classpath 'com.google.gms:google-services:4.4.2' // Asegúrate de tener la última versión
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}